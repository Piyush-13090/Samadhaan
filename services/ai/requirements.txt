# Runtime dependencies for the Samadhaan AI service.
#
# Deliberately minimal: only what the service actually runs today. ML libraries
# (torch, transformers, sentence-transformers, ultralytics, langchain) are added
# by the milestone that first needs them, so the image stays small until then.
# See docs/ML_PLAN.md for the planned model stack.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
structlog>=24.4.0
